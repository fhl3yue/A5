# 项目上下文恢复说明

本文档用于 Codex 上下文窗口压缩、开启新会话或交接队友时快速恢复项目状态。

## 1. 项目定位

项目题目方向为“景区导览服务 AI 数字人”。当前实现不是通用闲聊数字人，而是面向景区可配置知识库的导览服务平台。

当前演示景区为：灵山胜境。

核心表达建议：

- 标题保留为“景区导览 AI 数字人”。
- 页面显示“当前景区：灵山胜境”。
- 快捷问题来自当前景区知识库推荐问题。
- 管理后台支持切换或配置景区资料与数字人形象。

## 2. 项目目录与启动

项目根目录：

```powershell
D:\software
```

后端启动命令：

```powershell
powershell -ExecutionPolicy Bypass -File D:\software\start_backend.ps1
```

启动成功后访问：

- 前端页面：http://127.0.0.1:8000/app/
- 接口文档：http://127.0.0.1:8000/docs
- 健康检查：http://127.0.0.1:8000/api/health

## 3. 当前已完成能力

### A 端后端

已实现 FastAPI 后端、SQLite 数据库、基础知识库、文本问答、语音问答、语音合成、路线推荐、反馈提交、管理员登录、运营数据看板、日志查询、知识文档上传与数字人配置接口。

当前主要接口：

- `GET /`
- `GET /api/health`
- `POST /api/chat/text`
- `POST /api/chat/voice`
- `POST /api/recommend/route`
- `GET /api/digital-human/config`
- `POST /api/feedback`
- `POST /api/admin/login`
- `GET /api/admin/logs`
- `GET /api/admin/dashboard`
- `GET /api/admin/visitor-report`
- `POST /api/admin/digital-human/config`
- `POST /api/admin/docs/upload`
- `GET /api/admin/docs`
- `GET /api/admin/docs/{document_id}`
- `PATCH /api/admin/docs/{document_id}`
- `DELETE /api/admin/docs/{document_id}`
- `POST /api/admin/docs/{document_id}/reimport`
- `POST /api/admin/docs/{document_id}/chunks`
- `PATCH /api/admin/docs/chunks/{chunk_id}`
- `DELETE /api/admin/docs/chunks/{chunk_id}`

### B 端前端

已在 `D:\software\frontend` 实现静态前端，并由 FastAPI 挂载到 `/app/`。

游客端已包含：

- 智能导览问答。
- 文本输入。
- 语音录制与音频上传。
- 数字人回答时的表情与口型动画。
- 语音播放。
- 个性化路线推荐。
- 路线推荐支持预设选项和自定义输入。
- 游客满意度反馈。

管理端已包含：

- 管理员登录。
- 运营数据看板：当日游客、当日问答、满意度、热门问题、情绪分布、本周服务趋势、满意度趋势。
- 数据大屏概览：今日服务人次、本周服务人次、本周问答量、游客满意度、最高频问答、七日服务走势和满意度趋势。
- 游客感受度报告：关注点、七日情绪趋势、服务建议。
- 问答日志查看。
- 移动端问答日志卡片化展示，390px 宽度下无页面级横向溢出。
- 知识文档上传。
- 知识文档列表、预览、编辑、删除与重新导入。
- 知识片段新增、编辑与删除。
- 数字人形象配置表单。

## 4. 数字人形象管理状态

题目要求中“数字人形象管理：可配置数字人的外观、服装、声音等，使其更贴合景区文化特色”更偏向管理后台能力，不是游客自由换装。

当前实现方向：

- 管理者在后台配置数字人。
- 游客端读取当前景区数字人配置并展示。
- 配置项包括名称、角色标题、服务景区、服饰主题、授权角色素材模式、声音、欢迎语。
- 后端保存数字人配置。
- 文本问答和语音问答的 TTS 会使用后台配置的声音。
- 如果取得第三方角色授权，可将透明 PNG 放置为 `D:\software\frontend\assets\avatar\licensed-character.png`，并在后台服装主题中选择“授权角色素材”。

相关文件：

- `D:\software\backend\app\models.py`
- `D:\software\backend\app\schemas.py`
- `D:\software\backend\app\services\digital_human.py`
- `D:\software\backend\app\services\audio.py`
- `D:\software\backend\app\services\chat.py`
- `D:\software\backend\app\main.py`
- `D:\software\frontend\index.html`
- `D:\software\frontend\app.js`
- `D:\software\frontend\styles.css`

## 5. 当前限制与自纠结论

已经实现：

- 文本输入。
- 语音输入。
- 数字人语音回答。
- 表情变化。
- 播放状态下的口型动画。
- 后台数字人基础配置。
- 管理端移动端适配：配置表单、操作按钮、日志列表、知识库区域可在窄屏下阅读和操作。
- Playwright 浏览器端全流程检查：游客端问答、满意度反馈、路线推荐、管理端登录、看板、日志、知识库、数字人配置保存与恢复、移动端布局均通过。
- 管理端动态文本简体化展示：旧语音识别日志、热门问题、游客感受度报告和新问答结果会在展示层统一转为简体中文，避免简繁混排。
- Windows 演示版打包脚本与说明：`D:\software\scripts\build_windows_demo.ps1` 和 `D:\software\docs\release_packaging.md`，产物目录为 `D:\software\dist\scenic-ai-guide-demo`。

仍需如实说明的限制：

- 当前数字人是原创 SVG/CSS 分层动效角色，已升级为类 Live2D 虚拟偶像风格，但不是 Live2D Cubism SDK 模型。
- 项目不内置初音未来或其他第三方 IP 素材；如需使用，必须先取得权利方授权并自行提供授权素材文件。
- 当前口型同步是“语音播放状态驱动的嘴部动画”，不是逐音素级精准口型同步。
- 管理员鉴权仍是演示 token。
- 管理后台已补知识库管理、游客感受度报告、基础趋势看板和数据大屏概览，但仍可继续增强多景区配置和正式权限体系。

## 6. 如果开启新对话，先做这几步

请在新对话中让 Codex 先读取：

- `D:\software\docs\项目上下文恢复说明.md`
- `D:\software\docs\B端前端实现说明.md`
- `D:\software\docs\B端页面自纠检查记录.md`
- `D:\software\docs\A端开发步骤思路手册.md`

然后运行：

```powershell
Set-Location D:\software
git status --short
git log --oneline --graph --decorate --all -8
powershell -ExecutionPolicy Bypass -File D:\software\start_backend.ps1
```

浏览器检查：

- 打开 `http://127.0.0.1:8000/app/`
- 打开 `http://127.0.0.1:8000/docs`
- 测试 `POST /api/chat/text`
- 测试 `GET /api/digital-human/config`
- 测试 `POST /api/admin/digital-human/config`
- 需要完整自动化回归时，使用 Playwright 检查游客端问答、满意度、路线推荐、管理端登录、数据大屏概览、看板、日志、知识库、数字人配置保存与移动端 390px 布局。

## 7. 推荐下一步任务

优先级从高到低：

1. 在 GitHub 上从 `codex/scenic-ai-demo` 分支发起 Pull Request，合并到 `main`。
2. 演示前运行 `D:\software\reset_demo_env.ps1` 清理旧日志，避免历史语音识别残片影响展示。
3. 如需发行演示包，运行 `D:\software\scripts\build_windows_demo.ps1` 生成 Windows `.exe` 演示版。
4. 后续增强多景区配置表单、正式管理员鉴权和更精细的数字人口型同步。

## 8. 当前 Git 状态提醒

当前本地最新成果已提交并推送：

- 最新提交号：`d25ad19`
- 最新提交信息：`fix: normalize admin text to simplified chinese`
- 上一个提交：`4282fac feat: add admin operations overview screen`
- 打包提交：`a6e44ea build: add windows demo packaging guide`
- 移动端布局提交：`c58a464 fix: improve admin mobile layout`
- 核心演示 checkpoint：`c09a375`
- 本地分支：`main`
- 远程成果分支：`origin/codex/scenic-ai-demo`
- GitHub 仓库：`https://github.com/fhl3yue/A5`
- 当前确认：`main` 与 `origin/codex/scenic-ai-demo` 均指向 `d25ad19aa3cc6cb905ee0bcd4c433977058a20ba`。

注意：`origin/main` 上有本地没有的历史提交，直接 `git push origin main` 会被拒绝。不要强推覆盖远程 main。

如需继续分享当前成果，应使用：

```powershell
git push origin main:codex/scenic-ai-demo
```

如果该分支已存在并显示最新，则去 GitHub 页面创建 Pull Request。

如果开启新对话，先执行：

```powershell
cd D:\software
git status --short
git log --oneline --graph --decorate --all -12
```

确认 `d25ad19` 和 `origin/codex/scenic-ai-demo` 仍在同一提交上；若 `git status --short` 有输出，先判断是否为用户新增改动，不要直接覆盖。

## 9. 最近一次自动化调试结果

最新调试日期：2026-05-09。

已完成检查：

- `node --check frontend\app.js` 通过。
- `python -m compileall backend\app` 通过。
- `GET /api/health` 返回 `status: ok`。
- Playwright 登录管理端后确认“数据大屏概览”正常渲染。
- 390px 视口检查：`scrollWidth=390`，无页面级横向溢出。
- 管理端日志旧数据展示已统一简体化，截图中 `噴/觀/動/聖/週/點/體/準` 等繁体字已转为简体。
- 控制台无 `error` / `warning`。

上一轮完整调试日期：2026-05-08。

使用 Playwright 对 `http://127.0.0.1:8000/app/` 做浏览器端全流程检查，结果为 16 项全部通过：

- 页面标题、服务在线状态、桌面端横向溢出检查通过。
- 游客端文本问答、满意度反馈、路线推荐通过。
- 管理端登录、看板加载、问答日志加载、知识库列表加载通过。
- 数字人配置保存接口联动通过，测试后已恢复原欢迎语。
- 390px 移动端检查无页面级横向溢出，日志为卡片式展示。
- 控制台无 `error` / `warning`；仅有 Chrome 对密码框 `autocomplete` 的 verbose 提示。

注意：该自动化调试会新增问答日志和满意度反馈数据，演示前如需干净数据，可运行 `D:\software\reset_demo_env.ps1`。

## 10. 给队友复现的最短步骤

队友可直接拉取远程成果分支：

```powershell
git clone -b codex/scenic-ai-demo https://github.com/fhl3yue/A5.git D:\software
cd D:\software
python -m venv .venv
.\.venv\Scripts\pip install -r backend\requirements.txt
powershell -ExecutionPolicy Bypass -File D:\software\start_backend.ps1
```

启动后访问：

- `http://127.0.0.1:8000/app/`
- `http://127.0.0.1:8000/docs`

默认后台账号：`admin / admin123`。
